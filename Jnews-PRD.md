# PRD: 네이버 뉴스 검색 사이트

## 1. 개요

네이버 뉴스 검색 API를 활용한 뉴스 검색 웹 서비스. 매일 뉴스 보는 일이 지루하지 않도록, 귀엽고 재밌는 톤앤매너로 검색 경험을 제공하는 것이 핵심 목표.

- **목적**: 키워드 기반 뉴스 검색 + 결과 재탐색
- **차별점**: 딱딱한 뉴스 검색 UI가 아닌, 에디토리얼 매거진 톤 + 기하학적 마스코트 심볼과 마이크로 인터랙션이 있는 UI
- **배포 목표**: Vercel

---

## 2. 기술 스택

| 구분 | 선택 |
|---|---|
| 프레임워크 | Next.js (App Router) + TypeScript |
| 백엔드 | Next.js API Route 1개 (`app/api/news/route.ts`)가 네이버 API 프록시 역할 |
| 배포 | Vercel |
| 환경변수 관리 | `.env.local` (로컬), Vercel Environment Variables (배포) |
| 외부 API | 네이버 뉴스 검색 API (Open API) |

**API 키를 프론트엔드에 절대 노출하지 않는다.** 모든 네이버 API 호출은 서버(API Route)에서만 이루어지며, 클라이언트는 자체 API Route(`/api/news`)만 호출한다.

---

## 3. 핵심 기능

### 3.1 검색
- 검색창을 통한 키워드 검색
- 인기 키워드 칩: 고정 카테고리 리스트, 클릭 시 해당 키워드로 즉시 검색
  - *네이버 뉴스 API는 실시간 인기 검색어를 제공하지 않으므로 고정 리스트로 운영*
  - **칩 목록 (8개, 네이버 뉴스 기본 카테고리 기준)**: 정치, 경제, 사회, 생활문화, IT/과학, 세계, 스포츠, 연예
- **검색어 유효성 처리**
  - 빈 검색어(공백만 입력)는 검색 버튼 비활성화 또는 안내 메시지로 막음
  - 1글자 검색어는 허용 (제한 없음)

### 3.2 정렬
- **최신순** (네이버 API `sort=date`)
- **정확도순** (네이버 API `sort=sim`)
- 버튼 형태로 토글, 정렬 변경 시 결과 재요청

### 3.3 결과 내 재검색 (좁히기)
- 이미 받아온 검색 결과 안에서 추가 키워드로 다시 필터링하는 입력창
- 서버 재요청 없이 클라이언트 사이드 필터링으로 처리
- **필터링 범위**: 기사 제목 + 요약(스니펫) 모두에서 검색어 포함 여부 확인

### 3.4 기사 카드 리스트
- 제목, 요약, 언론사(있는 경우), 날짜, 카테고리별 대표 이미지(그레이스케일 처리) 표시
- **무한스크롤**: `IntersectionObserver`로 스크롤 끝 감지 → 다음 페이지 자동 로드, **한 번에 10개씩** 요청
- **카드 클릭 시 동작**: 모달로 요약 미리보기 표시 → 모달 내부에 "원문 보기" 버튼(새 탭 이동)

### 3.5 데이터 정제
- 제목/요약에 포함된 HTML 태그(`<b>` 등) 제거
- HTML 특수문자 엔티티(`&quot;`, `&amp;` 등) 디코딩
- 날짜를 한국식 포맷으로 변환 (예: `2026년 8월 18일`)

### 3.6 API 호출 및 캐싱 정책
- 네이버 뉴스 검색 API는 하루 호출 한도가 있으므로(개발자 계정 기준 통상 25,000회/일), 호출 절약을 위해 **서버 사이드 캐싱**을 적용
- 동일한 검색어 + 정렬 조건 요청은 **5분간** 서버 메모리(또는 간단한 캐시 저장소)에 저장해두고 재사용
- 5분 경과 후에는 네이버 API에 새로 요청하여 최신 결과로 갱신
- 카드 이미지용 Unsplash 검색(7장 참고)은 별도로 **24시간** 캐싱 — 무료 API 시간당 호출 한도(데모 앱 기준 50회)를 아끼기 위함. 뉴스 캐시와는 독립적인 캐시 저장소 사용

### 3.7 상태 화면
표정 없는 기하학적 마스코트 심볼이 **형태 변형(회전/분산/파편화)**으로 상태를 표현 (상세는 `DESIGN.md` 참고):

| 상태 | 설명 |
|---|---|
| 로딩 | 마스코트 심볼이 회전/펄스 애니메이션 + "기사를 찾는 중이에요" 메시지 |
| 결과 없음 | 마스코트 심볼이 반투명하게 흩어진 형태 + "이 키워드로는 아직 소식이 없네요" + 다른 검색어 제안 |
| 에러 | 마스코트 심볼이 비대칭으로 깨진 형태 + "잠시 문제가 생겼어요" + 재시도 버튼 |

---

## 4. 디자인 방향 (요약)

> 상세 디자인은 `DESIGN.md`에서 별도 논의 예정

- 딥네이비 + 머스타드/오렌지 포인트 컬러, 크림 배경, 그레이스케일 사진의 에디토리얼 매거진 톤
- 표정 없는 기하학적(로우폴리) 마스코트 심볼이 로딩·결과없음·에러 상태에서 형태 변형으로 반응
- 호버, 클릭 시 마이크로 인터랙션(모션)으로 재미 요소 추가
- 모바일 반응형 — PRD 확정 후 상세 논의

---

## 5. 폴더 구조

```
naver-news-search/
├── app/
│   ├── page.tsx              # 메인 페이지 전체 (검색창, 칩, 정렬, 재검색, 카드 리스트 조합)
│   ├── layout.tsx
│   ├── globals.css
│   └── api/
│       └── news/
│           └── route.ts      # 네이버 뉴스 API 프록시
├── components/
│   ├── NewsCard.tsx          # 카드(Unsplash 실사진 또는 글로시 3D 오브젝트 폴백) + 클릭 시 모달 포함
│   ├── ArticlePattern.tsx    # 절차적 글로시 오브젝트 렌더링 (Unsplash 폴백)
│   ├── HeadlineTicker.tsx    # 랜딩 전용 헤드라인 티커
│   ├── StatusView.tsx        # 로딩 / 결과없음 / 에러 마스코트 화면 (상태별 분기)
│   ├── Mascot.tsx             # 마스코트 SVG (상태/포즈 변형)
│   ├── CompanionMascot.tsx    # 화면에 상시 노출되는 동반자 마스코트 (커서 추적/idle)
│   └── Footer.tsx             # 다크 톤 푸터
├── lib/
│   ├── utils.ts                # 태그 제거, 날짜 변환, 네이버 API 호출 로직 통합
│   ├── pattern.ts              # 기사 제목 시드 기반 절차적 글로시 오브젝트 생성
│   └── unsplash.ts             # Unsplash 검색·24시간 캐싱·크레딧 정보
├── public/
│   └── mascot/                # 마스코트 이미지
├── .env.local
├── .gitignore
├── DESIGN.md
├── PRD.md
├── package.json
└── tsconfig.json
```

**구조 원칙**: 최대한 단순화. `SearchBar`/`KeywordChips`/`SortButtons`/`RefineInput`은 별도 파일 없이 `page.tsx` 내부 섹션으로 구현하며, 필요 시 추후 분리.

---

## 6. 보안

- 네이버 API `Client-ID`, `Client-Secret`, Unsplash `Access Key`는 모두 `.env.local`에 저장
- `.gitignore`에 `.env.local` 포함 → 깃허브 업로드 방지
- 배포 시 Vercel 프로젝트의 Environment Variables에 동일한 키를 등록 (자세한 절차는 별도 안내)

### 환경변수 목록 (예시)
```
NAVER_CLIENT_ID=xxxxxxxxxx
NAVER_CLIENT_SECRET=xxxxxxxxxx
UNSPLASH_ACCESS_KEY=xxxxxxxxxx
```

---

## 7. 향후 논의 예정 (본 PRD 범위 밖)

- `DESIGN.md`: 색상 팔레트, 타이포그래피, 마스코트 컨셉, 마이크로 인터랙션 상세
- 모바일 반응형 레이아웃 상세 설계
- Vercel 배포 및 환경변수 등록 절차
