# DESIGN.md — 뉴스 검색 사이트 디자인 가이드

## 1. 무드 요약

**"해외 신문을 읽는 커리어 우먼 / 패션 매거진" 톤의 에디토리얼 뉴스 사이트.**

- 귀엽지만 유치하지 않게 — 아기자기한 캐릭터 대신 **기하학적/로우폴리 심볼**로 포인트
- 그레이스케일 사진 + 강렬한 컬러 블록의 대비
- 비대칭 매거진 그리드, 넉넉한 여백
- 볼드한 헤드라인 타이포그래피

**참고한 무드보드 요소**
- Daily Routines: 흑백 사진 위 컬러 블록 오버레이, 넘버링 라벨(01/02/03)
- Elian Valerius: 화이트 배경 + 오렌지·머스타드 컬러 블록, 매거진식 여백
- The Halftimers: 어두운 히어로 + 사진 오버레이
- AD MEN: 로우폴리 기하학적 마스코트를 로고성 오브제로 활용

---

## 2. 컬러 팔레트

| 용도 | 색상 | HEX |
|---|---|---|
| 메인 (딥네이비) | 헤더, 헤드라인 텍스트, 주요 버튼 | `#06162B` |
| 포인트 (머스타드/오렌지) | 강조 버튼, 활성 칩, 링크 호버, 마스코트 | `#E08A2C` |
| 배경 (크림 오프화이트) | 전체 배경 | `#FAF6EF` |
| 텍스트 기본 | 본문 텍스트 | `#1A1A1A` |
| 텍스트 보조 | 메타정보(날짜, 언론사) | `#6B6B6B` |
| 보더/구분선 | 카드 테두리, 구분선 | `#E5E0D5` |
| 에러 포인트 | 에러 상태 강조 | `#C43D3D` |
| **시그니처 (네온 라임)** | **툭 튀는 포인트 전용** | **`#D4FF3D`** |

- 사진은 **그레이스케일 또는 듀오톤(네이비 틴트)** 처리하여 컬러 블록과 대비 *(→ 7장에서 사진 대신 절차적 패턴으로 대체됨에 따라 이 원칙은 카드 이미지 대신 패턴 배경색 대비에 적용)*
- 컬러는 절대 3개 이상 동시에 화면에서 강하게 쓰지 않기 (네이비 + 머스타드 두 가지가 기본)

### 네온 라임 사용 원칙

캐릿(careet.net) 스타일의 트렌디한 포인트로 도입. **전체 배경엔 절대 쓰지 않고**, 화면 전체 면적의 **5% 이내**로만 좁게 등장시켜 "툭 튀는 포인트"로 남긴다. 남발 금지.

허용되는 사용처:
- 상황 표시 텍스트(예: "· 방금")나 글로시 오브젝트의 스페큘러 하이라이트처럼 아주 작은 강조 포인트
- 링크·버튼 호버 시 밑줄 또는 테두리 색
- 마스코트가 검색 중일 때 은은한 글로우 효과
- 활성 상태(선택된 정렬 버튼 밑줄 등)의 강조 밑줄

금지: 배경색, 큰 면적의 채우기, 본문 텍스트 컬러로 사용.

---

## 3. 타이포그래피

| 용도 | 폰트 | 굵기 | 예시 크기(desktop) |
|---|---|---|---|
| 헤드라인 (사이트 타이틀, 섹션 제목) | Pretendard | Bold/ExtraBold | 32–48px |
| 기사 카드 제목 | Pretendard | SemiBold | 18–20px |
| 본문/요약 | Pretendard | Regular | 14–15px |
| 메타정보(날짜, 언론사) | Pretendard | Regular | 12–13px |
| 넘버링 라벨(포인트용) | Pretendard | Bold, 자간 넓게 | 12px, letter-spacing 0.1em |

- 헤드라인은 크고 임팩트 있게, 본문은 담백하게 — 대비를 확실히 준다
- 넘버링 라벨(`01`, `02`…)을 카테고리 칩이나 섹션 구분에 매거진처럼 활용 가능

---

## 4. 마스코트 가이드

**컨셉**: 로우폴리(저다각형) 기하학적 심볼. 표정이나 눈코입 없는 완전 추상 형태 (예: 접힌 종이비행기, 다각형 별 모양, 나침반형 오브제 등 하나의 심볼을 정해 일관되게 사용).

| 위치 | 사용 방식 |
|---|---|
| 헤더 로고 | 작은 사이즈로 상시 노출 |
| 로딩 화면 | 심볼이 천천히 회전 또는 펄스 애니메이션 + 은은한 라임 글로우(2장 참고) |
| 결과 없음 화면 | 심볼이 흐릿하게(반투명) 배치, "조각이 흩어진" 느낌으로 변형 |
| 에러 화면 | 심볼이 비대칭으로 깨진 형태(조각남)로 변형, 포인트 컬러는 에러 레드 `#C43D3D` 살짝 사용 |
| 동반자(플로팅) | 화면 한쪽에 상시 등장하는 별도 인스턴스 — 커서 추적/idle 흔들림/검색 시 포즈 변형 (10.1절 참고) |
| 푸터 | 헤더와 동일한 기본형으로 작게 |

- 표정·감정 표현 없이 **형태 변형(회전/분산/파편화)만으로 상태를 전달**하는 것이 원칙
- SVG 기반으로 제작하여 색상은 CSS 변수로 제어 (다크/라이트 대응 용이)

---

## 5. 레이아웃 / 그리드

- 데스크톱 기준 12컬럼 그리드, 최대 컨텐츠 너비 1200px
- 비대칭 배치 허용: 검색 히어로 영역은 전체 너비, 카드 리스트는 그리드형 매거진 배치
- 섹션 간 여백은 넉넉하게 (80–120px)
- 컬러 블록(네이비/머스타드)을 배경 섹션 구분용으로 부분 사용 (예: 히어로 영역 배경에 네이비 블록 + 사진 오버레이)

---

## 6. 컴포넌트 스타일

### 검색창
- 큰 사이즈, 하단 보더만 있는 미니멀한 형태 (박스형 X) 또는 크림 배경에 얇은 네이비 보더
- 포커스 시 보더 컬러가 머스타드로 전환

### 인기 키워드 칩
- 기본: 크림 배경 + 네이비 텍스트 + 얇은 보더
- 활성/호버: 머스타드 배경 + 화이트 텍스트

### 정렬 버튼 (최신순/정확도순)
- 텍스트 버튼 형태, 활성 상태는 머스타드 밑줄 또는 배경

### 기사 카드
- 블랙 또는 크림 단일 톤 배경 위에 절차적으로 생성된 글로시 3D 오브젝트(7장 참고)가 카드 대부분을 채운다
- 하단: 카테고리 캡션(작은 텍스트) → 구분선 → 기사 제목(볼드, 2줄, 배경에 맞춰 색 반전) → 우측 하단 원형 화살표(↗) 버튼
- 요약·언론사·날짜는 카드에 노출하지 않고 클릭 시 모달에서 보여준다 (미니멀한 카드 정보량 유지)
- 호버 가능 디바이스에서, 호버 시 오브젝트가 살짝 확대 + 카드 리프트(마이크로 인터랙션). 터치 디바이스에서는 탭 시 `:active`로 살짝 눌리는 느낌(scale 0.98)으로 대체 (6장 참고)

### 카드 클릭 모달
- 화면 중앙, 크림 배경, 상단에 대표 이미지
- 제목/요약 + "원문 보기" 버튼(머스타드 배경, 네이비 텍스트)

### 로딩 / 결과없음 / 에러
- 중앙 정렬, 마스코트 심볼 + 짧은 한 줄 메시지(재밌는 톤, 유치하지 않게)
  - 로딩: "기사를 찾는 중이에요"
  - 결과없음: "이 키워드로는 아직 소식이 없네요"
  - 에러: "잠시 문제가 생겼어요, 다시 시도해주세요" + 재시도 버튼

---

## 7. 카드 이미지 정책 — Unsplash 실사진 + 절차적 폴백

네이버 뉴스 API는 썸네일을 제공하지 않는다. **Unsplash Search API로 가져온 실제 사진**을 카드 이미지로 우선 사용하고, API 실패·한도 초과·결과 없음 상황에서는 기사 제목 시드 기반 **절차적 글로시 3D 오브젝트**(careet.net 스타일)로 자동 폴백한다. 폴백 로직은 절대 삭제하지 않는다 — Unsplash 없이도 사이트가 항상 정상적으로 보여야 한다.

### 실사진 검색 (Unsplash)
1. **키워드 추출**: 기사 제목에서 조사·특수문자를 제거하고 가장 긴 단어 하나를 뽑는 간단한 휴리스틱을 사용한다 (`lib/unsplash.ts`의 `extractKeyword`).
2. **검색어 조합**: 카테고리를 영어로 매핑(정치→politics, 경제→economy, 사회→society, 생활문화→culture, IT/과학→technology, 세계→world, 스포츠→sports, 연예→entertainment)한 뒤, 추출한 키워드와 함께 `"{카테고리 영문} {키워드}"` 형태로 Unsplash `/search/photos`에 요청한다. 영문 매핑이 없으면 카테고리 키워드만 사용.
3. **선택**: 검색 결과 중 하나를 기사 제목 해시 기반으로 결정적으로 선택 — 같은 기사는 항상 같은 사진, 검색어가 같은 여러 기사는 서로 다른 사진을 받을 수 있다.
4. **캐싱**: 동일한 검색어 조합의 Unsplash 결과는 **24시간** 서버 메모리에 캐싱한다 (뉴스 캐싱 5분과는 별도 정책 — 사진은 자주 바뀔 필요가 없고, Unsplash 무료 API의 시간당 호출 한도를 아끼기 위함).
5. **저작권 표기**: Unsplash API 이용약관에 따라 모달 안에 작게 "Photo by {촬영자명} on Unsplash" 크레딧을 표기하고, 촬영자 프로필로 링크한다.
6. **폴백**: API 키가 없거나, 요청이 실패하거나, 검색 결과가 없으면 `null`을 반환 — 카드/모달은 이 경우 아래의 절차적 글로시 오브젝트를 그대로 그린다.
7. 카드 위에 텍스트(캡션/제목)가 실사진과 겹치므로, 사진이 있을 때는 카드 하단에 어두운 그라디언트 스크림을 깔고 텍스트를 항상 밝은 톤으로 고정한다 (사진 내용에 따라 명암이 다르므로 안정적인 가독성 확보가 우선).

### 절차적 글로시 3D 오브젝트 (Unsplash 폴백)

로우폴리 삼각형(밋밋하다는 피드백) → 메시 그라디언트(오로라 느낌)를 거쳐, careet.net의 "블랙/크림 카드 위에 광택 있는 3D 오브젝트가 크게 떠 있는" 스타일을 CSS만으로 재현하는 현재 버전으로 정착했다. 이제는 Unsplash 실패 시의 폴백 역할이지만, 로직·품질은 동일하게 유지한다.

### 카드 배경 (폴백 모드)
- 카드 전체 배경은 **블랙(`#121212`) 또는 크림(`#FAF6EF`)** 중 하나. 기사 제목 해시로 결정되며, 서로 다른 제목이 섞인 리스트에서는 두 톤이 자연스럽게 번갈아 나오는 리듬감을 만든다.
- 이미지 영역과 텍스트 영역이 같은 배경색을 공유하는 **단일 톤 카드**로, 사진 썸네일 + 흰 여백 카드였던 이전 레이아웃과 다르다.

### 글로시 오브젝트
1. 기사 제목 문자열을 해시해 숫자 시드를 만든다 (char code 기반 해시, 예: djb2류 누적합).
2. 시드 기반 PRNG(mulberry32)로 아래를 **결정적으로** 뽑는다.
   - **형태**: ①단일 큰 구체(sphere) ②링(ring, 중앙을 CSS mask로 뚫어낸 도넛 형태) ③작은 구체 클러스터(4~6개, 일부는 blur 처리해 심도감 연출) 중 하나
   - **색상 조합**: 네이비/머스타드/크림 팔레트 내에서 베이스+하이라이트 조합 (예: 머스타드 베이스+크림 하이라이트, 네이비 베이스+라임 하이라이트). 라임은 항상 작은 스페큘러 하이라이트로만 등장하고 오브젝트의 큰 면적을 채우지 않는다 (2장의 "5% 이내" 원칙 유지)
   - **위치·크기·회전**: 카드마다 다르게, 대체로 크게(카드 폭의 55~82%) 배치
3. 광택은 `radial-gradient` 2겹을 겹쳐 연출 — 중심에서 살짝 벗어난 밝은 스페큘러 하이라이트 + 베이스 색 바디 셰이딩. `box-shadow`(inset + drop)로 입체감을 더한다.
4. 카드 전체에 아주 옅은(4% 불투명도) 노이즈 텍스처를 오버레이해 완전한 평면 느낌을 덜어낸다.
5. 같은 기사(제목)는 새로고침해도 항상 동일한 형태·색·위치. 다른 기사는 사실상 항상 다른 조합.
6. 순수 CSS(`radial-gradient` + `mask-image` + `box-shadow`)로 클라이언트에서 즉시 렌더링 — 이미지 파일, 외부 요청, 3D 렌더링 엔진 일절 없음.

### 카드 하단 레이아웃
카드 이미지(실사진 또는 폴백 오브젝트) 위에, 아래 순서로 텍스트를 배치한다.
1. 작은 캡션 텍스트 — 카테고리명. 실사진 모드는 항상 옅은 크림(스크림 위), 폴백 모드는 카드 배경에 맞춰 자동 반전(블랙 카드는 옅은 크림, 크림 카드는 회색)
2. 얇은 구분선 (같은 규칙으로 저대비 톤)
3. 기사 제목 — 볼드, 2줄, 실사진 모드는 항상 크림 텍스트, 폴백 모드는 배경에 맞춰 자동 반전
4. 카드 우측 하단에 **원형 화살표 버튼(↗)** — 머스타드 배경 고정. 카드 전체가 이미 클릭 영역이므로 시각적 CTA 역할만 하는 장식 요소로 둔다

### 폐기된 것
- `public/category/` 폴더와 카테고리별 사진 파일, 그리고 그레이스케일 필터(`filter: grayscale(100%)`) 처리 로직은 전부 제거한다 (실사진은 Unsplash에서 매번 받아오므로 로컬 파일이 필요 없다).

---

## 8. 모바일 반응형

- 카드 리스트: 데스크톱 그리드 → 모바일 1열 스택
- 검색창/칩: 가로 스크롤 가능한 칩 리스트로 전환
- 매거진식 비대칭 배치는 모바일에서 단순 수직 스택으로 단순화
- **스티키 검색바**: 스크롤 시 검색창이 화면 상단에 작게 축소되어 고정 (모바일에서 다시 검색하기 편하도록)
- **모달 → 바텀시트**: 데스크톱은 중앙 모달, 모바일에서는 화면 하단에서 올라오는 바텀시트 형태로 전환 (엄지로 닫기 편함)
- **반응형 타이포그래피**: 고정 px 대신 `clamp()` 사용해 화면 크기에 따라 헤드라인 크기가 자연스럽게 축소 (예: `font-size: clamp(24px, 4vw, 48px)`)

---

## 9. 최신 UI 트렌드 반영 (2025–2026)

과한 장식보다 **"상태를 알려주는 절제된 모션"**이 최근 트렌드라, 우리 사이트에 실제로 적용 가능한 요소만 선별했다.

### 9.1 마이크로 인터랙션
| 요소 | 동작 |
|---|---|
| 버튼 (검색, 원문보기 등) | 호버 시 살짝 확대(scale 1.02) + 그림자 진해짐, 클릭 시 살짝 눌리는 느낌(scale 0.98) |
| 키워드 칩 | 선택 시 배경색이 부드럽게(150ms) 전환, 클릭 시 미세한 바운스 |
| 카드 | 호버 시 그림자 깊어짐 + 3~5px 위로 살짝 이동 |
| 정렬 버튼 전환 | 활성 밑줄이 슬라이드하며 이동 (탭 전환 느낌) |

### 9.2 스크롤 리빌 애니메이션
- 카드가 화면에 들어올 때 아래→위로 살짝 슬라이드하며 페이드인 (stagger로 카드마다 약간의 시차)
- 무한스크롤에 이미 쓰는 `IntersectionObserver`를 그대로 활용해 구현 가능 (추가 라이브러리 없이도 가능)
- 과하지 않게: 이동 거리는 10~20px, 소요 시간은 300~400ms 이내로 짧게

### 9.3 스켈레톤 로딩
- 마스코트 심볼 + 함께, 카드 자리에 회색 블록(스켈레톤 UI)이 먼저 나타났다가 실제 콘텐츠로 교체
- 무한스크롤로 추가 로드할 때도 새 카드 자리에 스켈레톤이 짧게 나타남

### 9.4 접근성 고려 (Reduced Motion)
- 사용자 시스템 설정에 `prefers-reduced-motion: reduce`가 켜져 있으면 모든 애니메이션을 최소화(즉시 전환)하도록 처리
- 모션이 있어도 정보 전달에 필수적이지 않게 설계 (모션 꺼져도 기능은 동일하게 작동)

### 9.5 구현 방법 제안
- 대부분은 **CSS transition/animation만으로 충분** (버튼, 칩, 카드 호버, 스티키 헤더)
- 스크롤 리빌처럼 좀 더 정교한 효과가 필요하면 `Framer Motion` 라이브러리 사용 권장 (Next.js/React와 궁합 좋음, 가벼움)
- 무거운 3D나 WebGL 효과는 뉴스 검색 사이트 특성상 불필요 — 넣지 않음 (로딩 속도, 정보 접근성이 더 중요)

### 9.6 호버 vs 터치 분기 (모든 인터랙션 공통 원칙)

이 문서의 모든 마이크로 인터랙션·시그니처 인터랙션은 데스크톱 전용으로 설계하지 않는다. 반드시 아래 원칙으로 데스크톱/터치 양쪽을 함께 구현한다.

- 호버 효과(버튼 확대, 카드 그림자·이동 등)는 `@media (hover: hover)`로 감싸 **호버 가능한 디바이스에서만** 적용
- 터치 디바이스는 호버 대신 탭 시작 시 `:active` 상태로 살짝 눌리는 느낌(`scale(0.98)`)을 제공
- 터치 여부 판단은 `@media (hover: none)` (CSS) 또는 `window.matchMedia('(pointer: coarse)')` (JS)를 사용
- `:focus-visible`(키보드 포커스)은 호버 미디어쿼리와 무관하게 항상 살아있어야 함 (접근성)

---

## 10. 시그니처 인터랙션

"재미없고 밋밋하다"는 피드백을 반영해, 이 사이트만의 시그니처로 남을 인터랙션 4가지.

### 10.1 살아있는 마스코트

화면 한쪽에 마스코트 심볼이 (헤더 로고와 별개로) 상시 등장하는 작은 동반자 컴포넌트를 둔다.

- **데스크톱(호버 가능 디바이스)**: 커서를 약간의 딜레이를 두고 은은하게 따라다님 (lazy-follow — 커서에 딱 붙지 않고, 목표 지점을 향해 매 프레임 조금씩 보간하며 이동)
- **모바일/터치 디바이스**: 커서 추적이 불가능하므로, 가만히 있는 상태에서 자체적으로 미세하게 흔들리는 idle 애니메이션으로 대체
- **검색 실행마다**: 마스코트가 3~4가지 포즈(회전/점프/기울임/바운스) 중 하나를 랜덤 재생 — 데스크톱/모바일 공통
- **결과없음/에러**: 4장에서 정의한 대로 형태가 흩어지거나 깨지는 반응 그대로 유지 (이 반응은 결과 영역에 표시되는 마스코트에 적용되며, 동반자 마스코트와는 별개)
- 터치 디바이스 판단은 `window.matchMedia('(pointer: coarse)')` 또는 CSS `@media (hover: none)` 활용
- `prefers-reduced-motion`이 켜져 있으면 커서 추적/idle 흔들림/포즈 애니메이션 모두 생략, 정적으로 표시

### 10.2 타이포그래피 폭발 히어로

검색을 실행하는 순간, 입력한 검색어가 화면을 가득 채우는 거대한 타이포그래피로 0.6~0.8초간 등장했다가 축소되며 결과 리스트로 전환된다.

- 폰트 크기는 `clamp()`로 모바일 화면 폭에 맞게 자동 축소
- 배경은 딥네이비, 텍스트는 크림 — 히어로 영역과 톤을 맞춤
- `prefers-reduced-motion`이 켜져 있으면 이 효과를 생략하고 바로 결과로 전환

### 10.3 컬러가 살아있는 카드

7장의 절차적 글로시 오브젝트가 카드마다 다른 형태·색 조합으로 등장하고, 블랙/크림 배경도 번갈아 나오므로 스크롤할 때 카드마다 리듬감 있게 이어진다. 순수 CSS로 그려지므로 별도의 모바일 처리가 필요 없다.

### 10.4 캐릿 스타일 카피 톤

- 인기 키워드 섹션의 제목은 딱딱한 "인기 키워드" 대신 후킹성 카피를 쓴다. 후보 예시(구현 시 2~3개 중 하나를 랜덤 노출):
  - "요즘 이거 난리남"
  - "다들 이거 보고 있어요"
  - "지금 핫한 이야기"
- 카드에는 상황에 따라 작은 라임 텍스트를 선별적으로 붙인다. 예: 정렬이 "최신순"이고 오늘 날짜인 기사 상위 몇 개의 캡션 옆에 "· 방금". 모든 카드에 남발하지 않는다.

---

## 11. 랜딩(첫 화면) 경험

- 페이지 진입 시 빈 검색창만 던져놓지 않는다. 인기 키워드 칩 중 하나를 **자동으로 골라 검색을 실행**해, 진입 즉시 결과 피드를 노출한다.
- **구현 원칙 — 기본 피드는 특별한 케이스가 아니다.** 별도 컴포넌트나 별도 데이터 로직으로 분리하지 않고, 사용자가 칩을 클릭했을 때와 **완전히 동일한 검색 실행 함수**를 페이지 로드 시 한 번 자동으로 호출하는 방식으로 구현한다. 페이지네이션 상태(다음 시작 위치, 총 개수), 무한스크롤(`IntersectionObserver`), 로딩 상태를 전부 그대로 공유해야 한다 — 예전에는 기본 피드가 별도 경로였던 탓에 무한스크롤이 붙어있지 않은 버그가 있었다.
- 자동 검색이 아직 실행 중이거나, 사용자가 아직 직접 검색을 실행하지 않은 상태를 가리키는 가벼운 UI 플래그(예: "랜딩 모드")만 별도로 두고, 정렬/재검색 컨트롤 노출 여부와 상단 카피에만 사용한다. 데이터 로딩 로직에는 이 플래그가 관여하지 않는다.
- 사용자가 직접 검색(입력창 제출, 칩 클릭)하면 랜딩 모드가 해제되고 정렬·재검색 컨트롤이 나타난다.
- 랜딩 모드에서 상단 카피는 "검색 도구"가 아니라 "둘러봐도 되는 곳"이라는 인상을 주는 가벼운 문구를 쓴다. 후보:
  - "오늘 뭐 터졌나 보러 왔지?" (기본 적용)
  - "안 봐도 되는데, 보면 유익함"
  - "뉴스는 원래 재미없는데 우리는 다름"
- **헤드라인 티커**: 랜딩 모드에서만, 페이지 최상단(헤더보다 위)에 자동 검색으로 받아온 인기 기사 헤드라인이 좌우로 천천히 흐르는 얇은 티커 스트립을 노출한다 (증권 시세 티커 느낌). 각 헤드라인 클릭 시 원문으로 이동. 직접 검색을 실행하면 티커도 함께 사라진다. `prefers-reduced-motion`이 켜져 있으면 흐르지 않고 줄바꿈되는 정적 리스트로 대체한다.
- 진입 시 API 호출이 늘어나므로, 3.6절의 **5분 서버 사이드 캐싱 정책**을 반드시 함께 적용한다.

---

## 12. 푸터

현재 디자인에 누락되어 있던 푸터를 정의한다.

- **배경**: 딥네이비 통 배경 — 헤더/본문의 크림 배경과 대비되는 톤 반전
- **텍스트**: 크림 또는 화이트
- **구성**
  - 좌측: 마스코트 아이콘(작게) + 사이트명 + 한 줄 태그라인
  - 우측(또는 중앙): 최소한의 링크만 — 홈으로, 인기 키워드 바로가기
  - 하단: 저작권 문구, 연도는 자동 갱신 (`© {현재 연도} JNews`)
- **모바일**: 좌/우 구성을 세로로 스택
- 장식은 절제하고, 헤더와 톤은 맞추되 컬러만 반전(다크)한다
